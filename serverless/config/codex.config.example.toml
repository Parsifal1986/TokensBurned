[otel]
environment = "production"
log_user_prompt = false
exporter = { otlp-http = {
  endpoint = "https://otel.tokensburned.dev/v1/otel/logs",
  protocol = "json",
  headers = { Authorization = "Bearer REPLACE_WITH_DEVICE_TOKEN" }
} }

